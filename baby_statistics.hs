{-
	This script will calculate various simple, baby, statistics 
	in Haskell
-}

-- mean x = sum x / (length x)

--pop_var x = sum (x - mean x)^2 / length x

-- sample_var x = sum (x - mean x)^2 / (length x - 1)